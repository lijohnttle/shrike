import { makeStyles } from '@mui/styles';


const useStyles = makeStyles(() => ({
    sectionsRoot: {
        display: 'flex',
        flex: 'row no-wrap',
    },
    sectionRoot: {
        flexGrow: 1,
    },
}));


export { useStyles };
