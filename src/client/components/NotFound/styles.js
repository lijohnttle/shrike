import { makeStyles } from '@mui/styles';


const useStyles = makeStyles((theme) => ({
    contentRoot: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    },
}));


export {
    useStyles
};
